# 🚀 快速启动指南

## 一键启动所有客户端

已成功构建五子棋 Mono Repo 项目！所有服务已启动：

### 🌐 访问地址

| 框架 | 地址 | 特色 |
|------|------|------|
| **Vue 3** | http://localhost:5173 | 紫色渐变主题 + Composition API |
| **React 18** | http://localhost:5174 | 粉红渐变主题 + Hooks |
| **Angular 19** | http://localhost:4200 | 蓝色渐变主题 + Standalone Components |

### 📋 可用命令

```bash
# 同时启动所有客户端 (当前已运行)
pnpm dev

# 单独启动某个客户端
pnpm dev:vue       # 启动 Vue 版本
pnpm dev:react     # 启动 React 版本
pnpm dev:angular   # 启动 Angular 版本

# 构建项目
pnpm build:core    # 仅构建核心库
pnpm build:all     # 构建所有项目

# 查看导航页面
npx serve .        # 然后访问 http://localhost:3000
```

## ✅ 验收检查清单

### 1. 依赖关系验证 ✅

所有三个客户端的 `package.json` 中都包含：
```json
{
  "dependencies": {
    "@gomoku-monorepo/core-gomoku": "workspace:*"
  }
}
```

### 2. 核心功能测试

请在每个客户端中测试以下场景：

- [ ] **基本落子**: 点击棋盘任意空白位置，应显示黑子
- [ ] **回合切换**: 第二次落子应显示白子
- [ ] **非法落子**: 点击已有棋子的位置，不应有反应
- [ ] **横向胜利**: 连续落5个同色棋子（横向），应显示胜利信息
- [ ] **纵向胜利**: 连续落5个同色棋子（纵向），应显示胜利信息
- [ ] **斜向胜利**: 连续落5个同色棋子（斜向），应显示胜利信息
- [ ] **重置游戏**: 点击"重新开始"按钮，棋盘应清空

### 3. UI 特性验证

- [ ] **最后落子高亮**: 最后一步应有红色背景高亮
- [ ] **棋子落下动画**: 新棋子应有从上方掉落的动画效果
- [ ] **按钮悬停效果**: 鼠标悬停在按钮上应有平滑的提升效果
- [ ] **响应式设计**: 在不同窗口大小下都应正常显示

### 4. 逻辑一致性验证

在三个客户端中执行**完全相同**的落子序列，验证：

- [ ] 相同的落子顺序产生相同的胜负结果
- [ ] 相同的非法操作被同样拒绝
- [ ] 游戏状态转换完全一致

**测试序列示例:**
```
(7,7) → (7,8) → (8,7) → (8,8) → (9,7) → (9,8) → (10,7) → (10,8) → (11,7)
黑子应在此时获胜（列向五连）
```

## 🎨 设计亮点

### 视觉效果
- ✨ **渐变背景**: 每个框架使用独特的品牌色渐变
- ✨ **玻璃态设计**: 半透明毛玻璃效果的容器
- ✨ **微动画**: 棋子落下、按钮悬停、高亮闪烁等
- ✨ **3D 棋子**: 使用径向渐变和阴影模拟立体感

### 架构优势
- 🏗️ **关注点分离**: UI 和业务逻辑完全解耦
- 🏗️ **代码复用**: 一份逻辑，三个框架共享
- 🏗️ **类型安全**: 全栈 TypeScript，端到端类型检查
- 🏗️ **易于扩展**: 添加新框架只需实现 UI 层

## 🔧 故障排除

### Node.js 版本问题
如果遇到 Angular 构建错误，确保使用 Node.js >= 18.19:
```bash
fnm use 22.16.0
```

### 端口占用
如果端口已被占用，可以修改各客户端的端口：
- Vue: `packages/client-vue/package.json` → `"dev": "vite --port 5173"`
- React: `packages/client-react/package.json` → `"dev": "vite --port 5174"`
- Angular: `packages/client-angular/package.json` → `"dev": "ng serve --port 4200"`

### 依赖问题
重新安装依赖：
```bash
rm -rf node_modules packages/*/node_modules
pnpm install
pnpm build:core
```

## 📚 技术栈总结

| 技术 | 版本 | 用途 |
|------|------|------|
| pnpm | 10.x | Mono Repo 包管理 |
| TypeScript | 5.6.x | 类型安全 |
| Vue | 3.5.x | 客户端框架 |
| React | 18.3.x | 客户端框架 |
| Angular | 19.0.x | 客户端框架 |
| Vite | 6.x | 构建工具 (Vue/React) |
| tsup | 8.x | 库打包工具 |

---

**🎉 项目完成！享受跨框架的五子棋游戏吧！**
